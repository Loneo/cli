---
/**
 * Steps - Numbered step-by-step instructions
 * Usage:
 * <Steps>
 *   <Step title="Install">Run npm install</Step>
 *   <Step title="Configure">Edit config file</Step>
 *   <Step title="Deploy">Push to production</Step>
 * </Steps>
 */
---

<div class="steps-container not-prose">
  <slot />
</div>

<style>
  @reference "../../styles/global.css";
  .steps-container {
    @apply my-6 space-y-0;
  }
  
  .steps-container :global(.step-item) {
    counter-increment: step;
  }
  
  .steps-container {
    counter-reset: step;
  }
</style>
