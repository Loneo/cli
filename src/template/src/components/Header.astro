---
import { getConfigFile } from '../utils/config';
import ThemeToggle from './ThemeToggle.astro';

const config = await getConfigFile();
const { navbar } = config.navigation;
---

<header class="topbar">
  <div class="max-w-full mx-auto px-4 md:px-6 py-3 flex items-center justify-between">
    <!-- Mobile menu button -->
    <button 
      id="mobile-menu-btn" 
      class="md:hidden p-2 text-muted-foreground hover:text-foreground hover:bg-accent rounded transition-colors"
      aria-label="Open menu"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="4" x2="20" y1="12" y2="12"></line>
        <line x1="4" x2="20" y1="6" y2="6"></line>
        <line x1="4" x2="20" y1="18" y2="18"></line>
      </svg>
    </button>

    <!-- Logo -->
    <div class="flex items-center gap-4">
      <a href={config.branding.logo.href} class="flex items-center gap-3 hover:opacity-80 transition-opacity">
        <span class="text-2xl">📚</span>
        <h1 class="text-lg font-bold text-foreground m-0 tracking-tight hidden sm:block">
          {config.metadata.name}
        </h1>
      </a>
    </div>

    <!-- Desktop nav -->
    <nav class="hidden md:flex items-center gap-6">
      {navbar.links?.map((link) => (
        <a 
          href={link.href} 
          class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors"
          target={link.href.startsWith('http') ? '_blank' : undefined}
          rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
        >
          {link.label}
        </a>
      ))}
    </nav>

    <!-- Actions -->
    <div class="flex items-center gap-2">
      <!-- Search trigger -->
      <button 
        id="search-trigger"
        onclick="window.openSearchModal && window.openSearchModal()"
        class="hidden sm:flex items-center gap-2 px-3 py-1.5 text-sm text-muted-foreground rounded border border-border hover:bg-accent hover:text-foreground transition-colors"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"/>
          <path d="m21 21-4.3-4.3"/>
        </svg>
        <span>Search</span>
        <kbd class="px-1.5 py-0.5 text-[10px] font-mono bg-muted border border-border rounded">⌘K</kbd>
      </button>
      
      <ThemeToggle />
      
      {navbar.cta && (
        <a href={navbar.cta.href} class="cta-button hidden sm:inline-flex">
          {navbar.cta.label}
        </a>
      )}
    </div>
  </div>
</header>

